import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.busbay.app',
  appName: 'busbay',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
